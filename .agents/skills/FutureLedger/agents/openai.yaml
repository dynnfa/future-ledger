interface:
  display_name: "FutureLedger"
  short_description: "Repo-specific patterns and workflows for FutureLedger"
  default_prompt: "Use the FutureLedger repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true