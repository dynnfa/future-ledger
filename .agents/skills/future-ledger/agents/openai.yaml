interface:
  display_name: "Future Ledger"
  short_description: "Repo-specific patterns and workflows for future-ledger"
  default_prompt: "Use the future-ledger repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true